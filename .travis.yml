language: node_js
node_js:
  - "node"

install:
  - npm install  #安装hexo及插件

script:
  - hexo cl  #清除
  - hexo g  #生成

after_script:
  - cd ./public
  - git init
  - git config user.name "leslie-fang"
  - git config user.email "fangjiahao@sjtu.edu.cn"
  - git add .
  - git commit -m "Update"
  - git push --force --quiet "https://${GH_TOKEN}@${GH_REF}" master:master

branchs:
  only:
    - source

env:
 global:
   - GH_REF: github.com/Leslie-Fang/Leslie-Fang.github.io.git